import React from 'react';
import ReactDOM from 'react-dom';
import ReactTransitionComponent from './components/reactTransitionComponent';

ReactDOM.render(document.getElementById('app'), <ReactTransitionComponent />)