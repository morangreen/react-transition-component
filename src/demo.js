/* entry point for dev server*/
import React from 'react';
import {render} from 'react-dom';
import ReactTransitionComponent from './index';

render(<ReactTransitionComponent />, document.getElementById('app'));